// Ternary Operator

//a ? b : c
// a is condition , b true , c false

function findGreaterOrEqual(a, b) {
    return (a === b) ? "a and b are equal" 
      : (a > b) ? "a is greater" 
      : "b is greater";
  }